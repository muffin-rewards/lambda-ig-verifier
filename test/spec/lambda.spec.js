
describe('Verifier lambda', () => {

  it('responds with status 500 if token is missing')

  it('responds with status 422 if mode is not equal `subscribe` or challenge is missing')

  it('responds with status 401 if env and sent tokens don\'t match')

  it('responds with status 200 and puts challenge in body if everything is ok')

})
